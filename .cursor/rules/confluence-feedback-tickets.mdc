---
description: Triage Product Feedback Inbox on Confluence and create KAN Jira tickets for website and platform items
alwaysApply: false
---

# Confluence feedback → Jira tickets

Use when the user asks to triage feedback, create tickets from Confluence, or process the feedback inbox.

## Source page

- **Confluence space:** KAN (Nymphicus)
- **Page title:** `Product Feedback Inbox (Website + Platform)`
- **URL:** https://nymphicusai.atlassian.net/wiki/spaces/KAN/pages/2850818/Product+Feedback+Inbox+Website+Platform
- **Cloud:** nymphicusai.atlassian.net

Fetch with Atlassian MCP: `getConfluencePage` or `searchConfluenceUsingCql` → `title = "Product Feedback Inbox (Website + Platform)"`.

## Parse the Backlog table

Columns: **ID, Status, Where, Kind, Summary, Details, Name, Jira**.

(Legacy rows may still use old columns: Product, Type, Priority, Title, Area, Description, Steps, Expected / Actual, Reporter — map Product→Where, Type→Kind, Title→Summary, merge Description/Steps/Expected into Details, Reporter→Name.)

Process rows where **Status** is `new` or `ready` only. Skip `ticket-created`, `done`, `wont-fix`, and italic template/example rows (e.g. summary contains "One-line title" or "Paste your filled template").

## Create Jira issues (project KAN)

| Kind column | Jira issue type | Extra labels |
|-------------|-----------------|--------------|
| Bug (or bug) | Task | `bug` |
| Idea, suggestion, improvement | Story | — |
| Question, chore | Task | — |

Infer **priority** from Details/Summary if obvious (blocks work → high; nice-to-have → low; else medium).

**Where → labels:** `Website` → `website`; `Platform` → `platform`; `Both` → both labels.

**Summary format:** `[website] Summary` or `[platform] Summary` or `[both] Summary` (lowercase tag from Where)

**Description template:**

```markdown
**Feedback ID:** FB-xxx
**Where:** Website | Platform | Both
**Kind:** Bug | Idea | Question
**Reporter:** Name

## Summary
…

## Details
…

**Source:** Product Feedback Inbox (Confluence)
```

## After creating tickets

1. Update the Confluence row: `Status` → `ticket-created`, **Jira** → `KAN-xxx`.
2. Use `updateConfluencePage` with the full updated table body.
3. Report created keys to the user.

## Product scope

- **website** → nymphicus-website repo (marketing site, www.nymphi.ai)
- **platform** → app.nymphi.ai / The_Platform (agent builder, org admin, executions, MCP)
- **both** → create one ticket with both labels; note both surfaces in description

## Do not

- Create duplicate tickets for the same FB-xxx ID
- Ticket italic template rows (FB-004 placeholder row with "One-line title")
